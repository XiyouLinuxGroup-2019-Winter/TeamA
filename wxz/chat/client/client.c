#include "client.h"

void register()
{
    char username_buf[BUFSIZ];
    REGISTER_INFO user;
    while(1)
    {
        memset(&user,0,sizeof(REGISTER_INFO));
        user.flag=1;
        printf("请输入用户名:");
        
    }
    
}