
若调用wait,父进程执行到wait的时候有两种情况
1.子进程已变成僵尸进程，此时wait函数给子进程清除文件描述符,俗称“收尸”(子先于父)
2.子进程尚未变成僵尸进程(也就是还在运行)，那父进程阻塞直到有一个子进程结束，此时wait函数给子进程清除文件描述符(父先于子)

如果父进程不等待子进程结束，子进程僵尸进程，占用系统资源。
如果父进程等待子进程结束，将增加父进程的负担

SIFCHID SIG_IGN 可以使子进程结束后不会产生僵尸进程