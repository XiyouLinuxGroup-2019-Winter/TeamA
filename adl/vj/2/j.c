#include <stdio.h>
#include <string.h>

int main()
{
    int r, n;
    while (scanf("%d%d", &r, &n)==2&&(r!=-1||n!=-1))
    {
        int x[n];
        for (int i = 0; i < n; i++)
            scanf("%d", &x[i]);
  
        
        
    }
}