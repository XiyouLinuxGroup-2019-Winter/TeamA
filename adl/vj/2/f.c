#include <stdio.h>
#include<stdlib.h>
#
int main()
{
    int n,q;
    while (scanf("%d%d",&n,&q)==2&&(n!=0||q!=0))
    {
        for (int i = 0; i < n; i++)
        {
            cin>>s[i];
        }
        
    }
    
}